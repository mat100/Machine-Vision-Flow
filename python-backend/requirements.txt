# Production dependencies for Machine Vision Flow
# Install with: pip install -r requirements.txt
# Optimized for resource-constrained environments

# Web framework
fastapi==0.104.1
uvicorn==0.24.0  # Without [standard] extras to reduce dependencies
python-multipart==0.0.6

# Configuration & validation
pydantic==1.10.13  # v1 has BaseSettings built-in
python-dotenv==1.0.0

# Computer Vision
opencv-python-headless==4.8.1.78  # Headless version without GUI dependencies (~50MB smaller)
numpy==1.26.4  # Updated from 1.24.3 for security patches and performance improvements
